{
  "name": "v2ray-config-bot",
  "version": "1.0.0",
  "description": "Telegram bot to convert V2Ray links to various config formats",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.19.0"
  }
  {
     "type": "module"
   }
}
